name: maxdoors_erp
description: MaxDoors ERP — Flutter frontend (PocketBase backend bilan)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.2
  file_saver: ^0.3.1
  csv: ^6.0.0
  excel: ^4.0.6
  path_provider: ^2.1.4
  # State management
  provider: ^6.1.2

  # HTTP mijoz
  http: ^1.2.2

  # Mahalliy saqlash (JWT va rol)
  shared_preferences: ^2.3.2

  # Formatlar (sana/valyuta)
  intl: ^0.20.2
  fl_chart: ^1.0.0
  # Barcode skaneri (kamera yoki hardware skaner bilan)
  flutter_barcode_scanner: ^2.0.0

  # PDF yaratish va chop etish (nakladnoy, hisobotlar)
  pdf: ^3.11.0
  printing: ^5.13.4

  # iOS/Android uchun standart ikonka to‘plami (ixtiyoriy)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
    - assets/images/logo.png
    
  # Agar PNG/SVG rasmlar bo'lsa:
  # assets:
  #   - assets/images/logo.png

  # Agar custom shriftlar ishlatsangiz:
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_icon.png"